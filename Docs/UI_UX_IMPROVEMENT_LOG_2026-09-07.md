# UI・操作性改善記録（2026-09-07）

## 作業条件と復元地点

- 対象: Swift/macOS版 RINKAN UMIS。既存の画面構成、Adobe互換評価、Finderカラー、1物理pxのタイル間隔を維持。
- 改善前: `f5e521945ba37a4ea176a75ec9be18aa49000cee`、既存タグ `v0.2.0-alpha.5`。
- 作業ブランチ: `feature/ui-ux-20260907`。mainの変更、履歴書き換え、公証・配布版更新は今回の検証に含めない。
- 実カードの初期化・取り出し、実素材の変更は実行しない。ファイルを使う自動検証は専用一時fixtureに限定する。
- 夜間継続の区切り: 2026-09-07 08:00 JST。継続タスクは毎時、未解決の有用な改善を選んで進める。

## 調査結果と対応

| 調査で確認した問題 | 対応 | 検証状態 |
| --- | --- | --- |
| リネーム画面の入力・出力欄が左側に縮み、広い空白が生じる | 幅を使う入力・出力配置、手順番号、コピー前後の一覧、常設実行バーへ整理 | コンパイル済み・変更後の実画面未確認 |
| 取り込み開始が無効でも、次に必要な操作が分からない | ソース、対象、保存先、未割当、空フォルダ、処理待ちを順に案内。実行gateは維持 | コンパイル済み・変更後の実画面未確認 |
| スキャン後の「準備完了」が、取り込み準備も完了したように見える | 「素材を読み込み済み」へ変更 | 実装済み |
| スキャン警告の件数だけで詳細が見られない | 展開可能な警告一覧とコピー可能な本文を追加 | 実装済み・実画面未確認 |
| 中央の検索・件数・選択操作が1行に集中し、狭い幅で窮屈 | 検索と選択状況を2段に分離。選択件数、空状態からのソース選択を追加 | 実装済み・実画面未確認 |
| ステータスが小さく、失敗時は長い理由で表示が圧迫される | 表示領域・文字サイズ、成功/失敗アイコン、件数進捗、全文tooltipを整備 | コンパイル済み・変更後の実画面未確認 |
| 進捗など無関係な更新でも、選択全件をMainActorで再照合 | 選択revisionが変わる場合と一覧再構築時だけAppKit選択を同期 | 回帰テスト成功 |
| キーボードで検索欄や各画面へ移りにくい | 検索⌘F、画面切替⌃⌘1〜5、シーンパネル⌥⌘Iを追加。⌘Aは編集中のテキスト選択を優先 | コンパイル済み・実機キーボード未確認 |
| 画像プレビューが装飾扱いで、VoiceOverに意味が伝わらない | ファイル名を含む画像ラベル、メディア情報、エラー案内と再試行を追加 | コンパイル済み・実機VoiceOver未確認 |
| VoiceOverでタイルのプレビューは可能だが選択操作が不足 | 選択切替アクションと無効状態のAppKit反映を整備。古いAXアクションを保持されても処理中は拒否 | 実セルによる回帰テスト成功 |
| 画面切替時に不可視セルのサムネイル要求が残る可能性 | セル破棄時に未完了の要求をキャンセル | コンパイル済み。実メディアの長時間実測は未実施 |
| 評価画面の操作が横一列に集中、履歴の状態が分かりにくい | 評価とFinderカラーを分け、保存可否を表示。履歴件数・読込状態・再開制限を表示 | コンパイル済み・変更後の実画面未確認 |
| 独立レビューで、失敗理由と対応指示が別の情報であることを確認 | 失敗時は対応指示を維持し、異なる失敗理由も別行で表示。表示領域を可変高さ化 | 修正後の全テスト正常終了 |
| スキャン失効後も素材一覧が残ると、取り込み準備完了に見える | 失敗状態では取り込み開始を無効化し、必要な対応を案内 | 修正後の全テスト正常終了 |
| 選択済みソースが0件・読込失敗の場合に中央の再試行導線がない | 空状態を分類し「再スキャン」「別のフォルダ」を表示 | コンパイル済み・変更後の実画面未確認 |

## 検証記録

- 改善前ベースライン: `swift test --parallel`、289件成功（前段の検証記録）。
- 改善前の実画面: 取り込み、評価・タグ、フォルダリネームを確認。リネーム入力欄の縮みと空白、細かい表示を確認。
- 以降のMac状態: ロック中。CUAの自動解除ができず、変更後の実画面・実キーボード・VoiceOver検証は保留。画面ロックを回避する操作は行っていない。
- 統合時に引数順、既存テスト呼出の引数不足、テストのimport不足を検出して修正。並行編集によるコンパイラ中断も解消して再実行した。
- `swift test --parallel`: 293項目構成、終了コード0。大文字小文字を区別する専用APFSボリュームが未指定の1項目は環境条件によりskip（以下の追加実行でも確認）。
- 最終AXガード追加後の影響範囲検証: `AssetCollectionPerformancePolicyTests` 17件成功。大文字小文字APFSの環境条件確認1件skip、失敗0。
- 独立レビューの対応指示・失敗状態・空状態修正後、`swift test --parallel`を再実行: 293項目構成、終了コード0。
- `Vendor/AdobeXMP/Scripts/verify_xcframework.sh`: 成功、arm64/x86_64両sliceの同梱物を検証。
- `swift build --configuration release -Xswiftc -warnings-as-errors`: 最終ソースコミット`1c079e9`でも成功。
- `git diff --check`: 成功。

## ビルド成果物

- 最終ソースコミット: `1c079e9cfd2ea0c48eda8152772bdd3b8a1434c4`。
- 主変更コミット: `f3b509c`、独立レビュー修正コミット: `1c079e9`。
- アプリ: `dist/UI-QA-20260907/RINKAN UMIS.app`。
- ビルドマニフェスト: `dist/UI-QA-20260907/manifests/build-0.2.0-alpha.5-13-20260906T213154Z.txt`。
- build 13、macOS 13以降、Universal 2（`x86_64 arm64`）。両architectureのコンパイル・リンク成功。
- ビルド時の作業ツリーはclean。実行ファイルSHA-256: `13017ec2b823289c4593eecdec0e90a1332b8582269b3c7a9be298e0ecf4b8d2`。
- `codesign --verify --all-architectures --deep --strict`による独立再検証成功。
- ローカル検証用ad-hoc署名。Apple Developer ID署名・公証・配布用DMGは今回作成していない。既存の公証済み配布物は保持。
- Intel実機での起動、変更後の実画面、VoiceOverの操作通し試験は未実施。

## 次の検証

1. GitHubの`feature/ui-ux-20260907`へバックアップし、実行されるCIを確認する。
2. 実画面が利用可能なら、最小ウィンドウ・通常ウィンドウ、空状態、検索ゼロ件、複数選択、プレビュー再試行、長い日本語パスを確認。
3. 夜間の次回作業では、本書の残課題から有用な変更を選び、実装前に安全上の不変条件と検証方法を定める。

## 残る評価対象

- 大量素材の検索は現在も同期filter。非同期化には表示と操作対象の一致を保つ設計・実測が必要。
- 選択集合のUI用intersection、評価toolbarの集計、巨大動画1本のコピー中の進捗更新頻度も実測対象。今回のrevision修正だけで全MainActor負荷がなくなったとはしない。
- プレビューでの前後素材移動は、動画プレイヤーの終了確認と表示中一覧・選択状態を一体で設計する必要があるため未追加。
- カード初期化・取り出しの製品用backend認定、再起動後の取り込み復旧UI、Copy and Renameのcrash recovery UIは別途設計・受入検証が必要。UI改修によって利用可能とはしない。
- 実SD・NAS・実Adobe製品・Intel実機の外部検証は、本Mac上の単体テストやビルド成功と区別する。
